super + w
   firefox

super + space
   st 2>/dev/null
    
super + p
    dmenu_run

# bug with npm
super + r
    st -e bash -c "ranger 2>/dev/null; $SHELL"

super + alt + n
    nt dmenu

super + alt + shift + n
    ant dmenu

Print + shift
	flameshot gui

mode_switch + c
	copyq toggle

super + shift + o
    passmenu

mode_switch + e
	emoji

# lsp not working
super + shift + x
	st -e nvim ~/.config/xmonad/xmonad.hs

super + Escape
    nw dmenu 

super + shift + Escape
    bookpractice -d 

super + o
    simplescreenrecorder

super + shift +  p
    xrandr-dmenu

super + F1
    browseCheatsheets

control + shift + Escape
    pskill

super + F9
    mountDrive

super + F10
    unmountDrive

XF86AudioMute
     volume toggle; $APPS/dwm/refreshVolume

XF86AudioRaiseVolume
     volume up; $APPS/dwm/refreshVolume

XF86AudioLowerVolume
     volume down; $APPS/dwm/refreshVolume

XF86MonBrightnessUp
    xbacklight -inc 10

XF86MonBrightnessDown
    xbacklight -dec 10

XF86MonBrightnessUp
    xbacklight -inc 10

XF86MonBrightnessDown
    xbacklight -dec 10

XF86TouchpadToggle
	synclient TouchpadOff={1,0}

XF86AudioPlay
	mpc toggle
