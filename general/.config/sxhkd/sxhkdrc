super + space
   st 2>/dev/null
    
super + p
    dmenu_run

super + shift + o
	~/.config/sxhkd/dm_p 2>/dev/null

Print + shift
	flameshot gui

mode_switch + c
	copyq toggle

# lsp not working
super + shift + x
	st -e nvim ~/.config/xmonad/xmonad.hs

super + Escape
    browseWiki -d 

super + shift + Escape
    bookpractice -d 

super + o
    simplescreenrecorder

super + shift +  p
    xrandr-dmenu

super + F1
    browseCheatsheets

control + shift + Escape
    pskill

XF86AudioMute
     $SCRIPTS/shared/volume mute; $APPS/dwm/refreshVolume
     # pactl set-sink-mute 0 toggle

XF86AudioRaiseVolume
     $SCRIPTS/shared/volume up; $APPS/dwm/refreshVolume
     # pactl set-sink-volume 0 +5%

XF86AudioLowerVolume
     $SCRIPTS/shared/volume down; $APPS/dwm/refreshVolume
     # pactl set-sink-volume 0 -5%

XF86MonBrightnessUp
    xbacklight -inc 10

XF86MonBrightnessDown
    xbacklight -dec 10

XF86MonBrightnessUp
    xbacklight -inc 10

XF86MonBrightnessDown
    xbacklight -dec 10

XF86TouchpadToggle
	synclient TouchpadOff={1,0}

XF86AudioPlay
	mpc toggle
