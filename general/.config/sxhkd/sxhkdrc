super + w
    firefox

super + x
    firefox excalidraw.com

# key names can be found in xev
super + BackSpace
   st 2>/dev/null
    
super + p
    dmenu_run

mod5 + shift + o
    sh -c 'sleep 0.5; xdotool type "$(xclip -o -selection primary)"'

super + alt + 1
  pomo start 1h

super + alt + 2
  pomo start 2h

super + alt + 3
  pomo start 3h

super + alt + 0
  pomo stop

# bug with npm
super + r
    st -e bash -c "ranger 2>/dev/null; clear; $SHELL"

super + alt + n
    nt dmenu

super + alt + shift + n
    ant dmenu

Print + shift
  SESSION_MANAGER="" flameshot gui 2>/dev/null
  # flameshot gui

mod5 + c
	copyq toggle

super + shift + o
    passmenu

mod5 + shift + e
	emoji

super + shift + x
	st -e nvim ~/.config/xmonad/xmonad.hs

super + Escape
     nw dmenu 

super + shift + Escape
    browseCheatsheets

super + o
    simplescreenrecorder

super + shift + p
    xrandr-dmenu

super + F1
    bookpractice -d 

control + shift + Escape
    pskill

super + F9
    mountDrive

super + F10
    umountDrive

control + shift + space
    $UTILITIES/misc/toggle_layout

XF86AudioMute
     volume toggle; $APPS/dwm/refreshVolume

XF86AudioRaiseVolume
     volume up; $APPS/dwm/refreshVolume

XF86AudioLowerVolume
     volume down; $APPS/dwm/refreshVolume

XF86MonBrightnessUp
    xbacklight -inc 10

XF86MonBrightnessDown
    xbacklight -dec 10

XF86MonBrightnessUp
    xbacklight -inc 10

XF86MonBrightnessDown
    xbacklight -dec 10

XF86TouchpadToggle
	synclient TouchpadOff={1,0}

XF86AudioPlay
	mpc toggle
