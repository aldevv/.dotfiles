#!/bin/bash
# Get the latest tag in a git repository from remote

git fetch --tags --quiet 2>/dev/null

git tag --sort=-v:refname | head -1 || {
    echo "No tags found in this repository" >&2
    exit 1
}
